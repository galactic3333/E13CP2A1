<%= form_with model: @todo, local:true do |f| %>
	<%= f.label :description, "Descripción" %>
	<%= f.text_field :description %>
	<%= f.label :completed, "Todo completado?" %>
	<%= f.text_field :completed %>
	<%= f.submit "Enviar" %>
<% end %>